--- a/HvDecoderMs/decoderms.h	2026-03-18 08:24:58.000000000 +0000
+++ b/HvDecoderMs/decoderms.h	2026-05-18 10:04:39.713009794 +0000
@@ -169,9 +169,8 @@
     QString msg;
 }
 xcopy_struct;
-class DecoderFt8 : public QObject
+class DecoderFt8
 {
-    Q_OBJECT
 public:
     explicit DecoderFt8(int id);
     ~DecoderFt8();
@@ -193,9 +192,14 @@
     void SetVarDecodeFtPar(bool,int,int);
     void SetFreqGlobal(QString);//2.76.5
 
-signals:
-    void EmitDecodetTextFt(QStringList);
-    void EmitBackColor();
+public:
+    // FFI hook — replaces the Qt signal EmitDecodetTextFt(QStringList).
+    // Invoked once per decoded message with the 8-field result list.
+    typedef void (*Ft8ResultFn)(void *ctx, const QStringList &fields);
+    void SetResultCallback(Ft8ResultFn fn, void *ctx) { m_result_fn = fn; m_result_ctx = ctx; }
+private:
+    Ft8ResultFn m_result_fn = nullptr;
+    void *m_result_ctx = nullptr;
 
 private:
     static QString s_MyBaseCall8;
--- a/HvDecoderMs/decoderft8.cpp	2026-03-19 08:41:26.000000000 +0000
+++ b/HvDecoderMs/decoderft8.cpp	2026-05-18 10:04:51.350893233 +0000
@@ -2538,7 +2538,7 @@
         if (f_new_p)
         {
             f_new_p = false;
-            emit EmitBackColor();
+            /* EmitBackColor(): UI-only signal, dropped for FFI build */
         }
         float qual=1.0-(nhr+dmi)/60.0; //scale qual to 0.0-1.0
         if (qual_eq_dmin)//222 iaptype==8
@@ -2560,7 +2560,7 @@
         <<message<<str_iaptype
         <<QString("%1").arg(qual,0,'f',1)
         <<QString("%1").arg((int)f1);
-        emit EmitDecodetTextFt(list);//1.27 psk rep fopen bool true, false no file open
+        if (m_result_fn) m_result_fn(m_result_ctx, list);//FFI callback (was: emit EmitDecodetTextFt)
         have_dec = true; //qDebug()<<message.indexOf(s_HisCall8)<<s_HisCall8;
         if (s_HisCall8.count()>2 && message.indexOf(s_HisCall8)>-1) s_ltry_a8=false;//ws300rc1 if(index(msg37,trim(hiscall12)).gt.0) la8=.false.
     }
